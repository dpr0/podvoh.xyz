h1.title = @category.name

- @category.subcategories.each do |subcategory|
  = link_to subcategory.name, section_category_subcategory_path(Section.podvoh, @category.id, subcategory.id), class: 'button is-info'
